# TURN TLS sertifikaları

Kurumsal TURN sunucusunda `turn.example.gov.tr` için geçerli `fullchain.pem` ve `privkey.pem` dosyalarını bu klasöre secret/volume olarak bağlayın. Sertifikaları Git'e eklemeyin.
